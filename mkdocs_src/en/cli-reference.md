# CLI Reference

<!-- TODO(docs:cli-reference): Document every forge CLI subcommand (backtest / optimize / wft / data, etc.)
     with parameters, output examples, and error codes by reading the alpha-forge source. -->

A catalog of subcommands provided by the AlphaForge CLI (`forge`).

## TODO

- `forge backtest run`
- `forge optimize bayes`
- `forge optimize grid`
- `forge wft`
- `forge data`
